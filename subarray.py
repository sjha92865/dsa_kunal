# subarray is not taught by kunal
# subset, subsequence is same, both can elements like (a,b,c)--> ac, maintains order.
# but subarray should have continuos & same order elements only(bc,ab).
# check subarray using recursion and without recursion.

